dependencies {
    implementation project(':soda-components:soda-component-adapter-starter-web')
    implementation project(':soda-components:soda-component-adapter-starter-job')
    implementation project(':soda-components:soda-component-adapter-starter-consumer')
    implementation project(':soda-user:soda-user-api')     // compile time — UserService interface, Commands, DTOs
    runtimeOnly project(':soda-user:soda-user-application')    // runtime only — Spring bean injection
    implementation 'org.springframework:spring-tx'         // consumer 的 @TransactionalEventListener（AFTER_COMMIT）
}
