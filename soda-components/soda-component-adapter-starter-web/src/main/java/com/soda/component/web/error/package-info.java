/**
 * 错误响应内部实现 —— 异常 → RFC 9457 ProblemDetail 翻译（见 ADR-0013 / ADR-0015）。
 * <p>
 * 由 Spring 自动发现，业务模块不直接引用。
 */
@NullMarked
package com.soda.component.web.error;

import org.jspecify.annotations.NullMarked;
